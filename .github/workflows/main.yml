name: Demo workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo gt actions!
      - name: print a few lines
        run: |
          echo first line
          echo second line

          
